createPerson: >
    insert into person (first_name, middle_name, last_name, gender, in_actors_equity)
    values (?, ?, ?, ?, ?);

deletePerson: delete from person where id = ?;

updatePerson: update person set ? where id = ?;

readPersonById: select * from person where id = ?;